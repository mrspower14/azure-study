#0. Dockerfile 확인
#1. 도커 이미지 빌드
docker image build ./firstNodeDockerApp -t firstnode-app:1.0
docker build ./guestbook -t guestbook-app 
## -t: tag 
#amd64 버전으로.
docker build ./guestbook -t guestbook:v2

#2. 이미지 확인
docker images

###vscode docker   확장 install



## platform 이 다를 경우 다시 빌드 
docker build --platform=linux/amd64 -t crshk.azurecr.io/guestbook:v2 .
docker push crshk.azurecr.io/guestbook:v2


